<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.wsgroup.main">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WSGroup"
        android:usesCleartextTraffic="true">
        <activity android:name=".views.activities.OrderActivity"></activity>
        <activity android:name=".views.activities.account.DeliveryAddressActivity" />
        <activity android:name=".views.activities.ordering.OrderInfoActivity" />
        <activity android:name=".views.activities.ordering.OrderConfirmActivity" />
        <activity android:name=".views.activities.SupplierActivity" />

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_camera" />
        <!--
 Set color used with incoming notification messages. This is used when no color is set for the incoming
             notification message. See README(https://goo.gl/6BKBk7) for more.
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/black" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="Test message" />

        <activity android:name=".views.activities.CartActivity" />
        <activity android:name=".views.activities.account.AccountInformationActivity" />
        <activity android:name=".views.activities.ProductDetailActivity" />
        <activity android:name=".views.activities.account.AccountDetailActivity" />
        <activity android:name=".views.activities.account.PasswordChangeActivity" />
        <activity android:name=".views.activities.account.OTPConfirmActivity" />
        <activity android:name=".views.activities.account.PhoneInputActivity" />
        <activity android:name=".views.activities.account.SignInActivity" />
        <activity
            android:name=".views.activities.MainActivity"
            android:windowSoftInputMode="stateAlwaysHidden|adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".models.services.FirebaseMessageService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>